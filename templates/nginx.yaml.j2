services:
    nginx:
        container_name: nginx
        restart: unless-stopped
        image: nginx:alpine
        hostname: nginx
        network_mode: host
        volumes:
            - ./config:/etc/nginx/conf.d
            - ./ssl:/etc/nginx/ssl
